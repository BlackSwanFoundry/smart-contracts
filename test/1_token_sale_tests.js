const TokenSale = artifacts.require("bsc/TokenSale");
const SimpleToken = artifacts.require("bsc/SimpleToken");
contract("TokenSale", accounts => {
    it("Should ")
    it("Schedule token sale.", () => {
        TokenSale
        .deployed()
        .then(instance => instance.create())
    });
});